# A股桌面盯盘挂件

> 半透明、置顶、可拖动的 Windows 11 桌面小挂件。最多盯 5 只 A 股，实时刷新，带分时走势缩略图，顶部常驻大盘指数，涨跌超阈值自动提醒。

当前版本：**v2.1.0** · 更新于 2026-09-19 · [下载](https://github.com/Fallenathenaaa/astock-widget/releases/tag/v2.1.0)

---

## 效果预览

### 收盘彩蛋（v1.2.0+）

![收盘彩蛋](screenshots/preview-effect-fire-frost.png?v=3)

每天 **14:57** 集合竞价那一刻定格一次：

- 涨幅超过阈值的股票，格子**燃烧** —— `#FF5500` 橙红底色 + 12 颗上升的火星余烬
- 跌幅超过阈值的股票，格子**结霜** —— 《冰雪奇缘》配色：淡青 → 蓝绿 → 浅蓝灰渐变，中心爱莎蓝魔法光晕，`#FCFFFF` 极浅暖白当反光与雪花

两种状态都持续 **3 分钟**，之后自动恢复。0% 透明模式下依然清晰可见。

| 0% 透明 · 浅色壁纸下的彩蛋 |
|---|
| ![ghost effect](screenshots/preview-effect-ghost-light.png?v=3) |

### 默认外观（75% 透明度）

![75% 透明度](screenshots/preview-normal.png?v=3)

深灰半透明面板，红涨绿跌，右侧是分时迷你走势图。

### 多选删除股票（v1.1.0）

![多选删除](screenshots/preview-multi-select.png?v=3)

右键任意一只股票、或长按 0.5 秒，即可进入多选模式。每行左侧出现复选框，底部出现 `删除(N)` 和 `取消` 按钮。点击行切换勾选，按 Esc 或右键空白退出。

### 不同透明度对比

| 0% 透明 · 深色壁纸 | 0% 透明 · 浅色壁纸 |
|---|---|
| ![ghost dark](screenshots/preview-ghost-dark.png?v=3) | ![ghost light](screenshots/preview-ghost-light.png?v=3) |

| 100% 不透明 |
|---|
| ![opaque](screenshots/preview-opaque.png?v=3) |

**0% 透明模式**（俗称"幽灵模式"）下背景完全消失，只剩文字浮在壁纸上。此时程序会：

- 自动给所有文字加一层**描边阴影**，防止在浅色壁纸上糊掉
- 隐去分隔线、涨跌幅色块、边框（只留文字）
- 分时基准虚线改为深色
- 搜索框自动切换成**深色底**，保证在浅色壁纸上依然可见

窗口照常可点击、可拖动。

### 界面缩放

| 85% 缩放 | 130% 缩放 |
|---|---|
| ![85](screenshots/preview-85.png?v=3) | ![130](screenshots/preview-130.png?v=3) |

等比缩放——字号、行高、间距、窗口整体一起变，不会出现"字大了挤成一团"。可选 85% / 标准 / 115% / 130%。

### 搜索加自选

![search](screenshots/preview-search.png?v=3)

标题右边的输入框支持拼音缩写、全拼、代码、中文名四种输入。

---

## 版本特点

### v2.1.0（当前）

**正确性加固 + 数据源模块化**

- 🆔 **股票身份用完整代码**：`sh000001`（上证指数）和 `sz000001`（平安银行）的 6 位代码都是 `000001`，以前按下标/代码配对会串股。现在身份直接来自接口返回的变量名，漏返回一只或返回顺序变了都不会错位
- ⏱ **异步结果有版本号**：换自选股后旧的行情会被丢弃；搜索结果带关键字和编号，改了词或按了 Esc 之后旧结果不会再弹回来
- 🕘 **一律北京时间**：系统时区被改过、或者在海外，也不会把实盘判成休市；补了 2026 年官方休市日表
- 🔔 **异动提醒不再重复**：只在交易时段提醒，且是边沿触发（回到安全区才重新武装），收盘后不会每 60 秒弹同一条
- 🎆 **收盘彩蛋改成窗口触发**：14:57–15:00 内第一次拿到当天行情就触发，不再只认 14:57 那一分钟
- 🧩 **双数据源**：腾讯主源 + 新浪备源，主源挂了自动降级；搜索结果只留沪深京 A 股 / 指数 / ETF
- 🧪 **测试从干净 checkout 也能跑**：不再依赖开发者机器上那份 `stocks.json`
- 🔍 **发布卫生检查**：扫私有路径 / token / 带凭据 URL，并用 AST 检查"同一个类里有没有同名方法"

### v2.0.1

**安全收紧 + 文档图修复**

- 🧹 **退出时清空手动开关**：退出即清，下次启动严格按日期触发
- 🔧 **修单实例 mutex 泄漏**：之前检测到已存在时漏 CloseHandle，导致别人退出后**永远误报"已在运行"**
- 🖼 **修文档图乱码**：offscreen 渲染时中文 + emoji 全方块，新增 `tools/detect_garbled.py` 程序化扫，文档工具注入系统字体后重生成
- 🔒 **文档脱敏**：内部细节从公开文档移除

### v2.0.0

**内部重构 + 性能瘦身**

- 🧹 **去屎山**：删掉重复判断方法、123 行旧画法代码、没用函数和 import
- ⚡ **降 CPU**：空闲心跳 200ms→500ms，动画 25fps→20fps，隐藏到托盘时**完全不画**，`QColor` 全部走缓存
- 📦 **测试入库**：149 项检查收进 `tests/`，`python run_tests.py` 一把跑完

### v1.4.13

**配置读坏了会自动捞回来**

| 特性 | 说明 |
|---|---|
| 🆘 **自动回滚** | `stocks.json` 解析失败（手改 JSON 写坏了 / 写一半断电）时，从 `backups/` 里挑**最近一份能用的**自动恢复，托盘提示用的是哪份 |
| 🧯 **坏文件留着** | 坏掉的原文件**原样保留**，不覆盖不删除，方便你自己翻 |
| 🚯 **垃圾不进备份** | 坏 JSON 不会被存进 `backups/`——不然会占掉备份位，还可能被下次恢复选中 |
| 🆕 **全新安装不翻旧账** | 文件根本不存在时（第一次用）走默认配置，不会把删掉的旧配置复活 |

### v1.4.12

两个从真实使用里撞出来的修复：

| 特性 | 说明 |
|---|---|
| 🪟 **认窗口不看标题** | 单实例唤醒改为**枚举所有同名窗口 + 校验进程是不是 `python(w).exe`**。原来用 `FindWindowW(标题)`，而标题是用户自己设的——实测撞上了**微信会话窗口**（微信也是 Qt 写的，连窗口类名都一样），会把微信弹到前台 |

### v1.4.11

**拖动吸附屏幕边缘**

| 特性 | 说明 |
|---|---|
| 🧲 **磁吸** | 拖到距屏幕边缘 16px 内就自动贴上去，四边四角都吃。松手时再吸一次，保证落位 |
| 🖥 **多显示器** | 按**光标所在**那块屏算（不是窗口原来那块），双屏拖到哪吸哪 |
| 🔘 **可关** | 右键 →「拖动吸附屏幕边缘」；打开时立刻吸一次，不用手动再拖一下 |

### v1.4.10

**崩溃兜底：出错不再"静默消失"**

| 特性 | 说明 |
|---|---|
| 🪵 **崩溃日志** | 挂件用 `pythonw` 启动、没有控制台，以前一崩就只剩"窗口不见了"。现在未捕获异常一律落盘到 `logs/crash-*.log`（含版本、Python 版本、完整 traceback），留最近 10 份 |
| 🔔 **弹窗提示** | 出错时弹系统提示框，告诉你日志在哪。用 Win32 `MessageBox` 而不是 Qt 的，Qt 自己崩了也弹得出来 |
| 🕸 **三层兜底** | `sys.excepthook`（主线程 / Qt 槽）+ `threading.excepthook`（子线程）+ 行情/搜索线程内部各包一层 —— QThread 里的异常不一定走 excepthook |
| 🚑 **启动保护** | `main()` 整体包 try/except，启动阶段炸了也有日志，不会开天窗 |

### v1.4.9

**配置自动备份 + 单实例保护**

| 特性 | 说明 |
|---|---|
| 💾 **配置自动备份** | 每次写配置前，先把「改动前」那份存进 `backups/`；每次启动也存一份。持仓成本被误删也能捞回来 |
| ⏱ **一键回滚** | 右键 →「配置备份」→ 列出最近 8 份，点哪份回到哪份。覆盖前会自动把当前状态也存一份，方便反悔 |
| 🧹 **只留最近 30 份** | 超出自动删最旧的。只挪窗口位置（`pos`）不算实质变化，不会刷屏 |
| 🚦 **单实例保护** | Windows 命名 mutex：重复启动时不再开第二个窗口，而是把已在运行的那个唤到前台并提示 |

### v1.4.8

**单实例保护**

| 特性 | 说明 |
|---|---|
| 🚦 **单实例保护** | 重复启动时提示并唤醒旧实例，不再叠窗口 |

### v1.4.6

**绘制重构**

| 特性 | 说明 |
|---|---|
| 🧩 **绘制路径整理** | 抽出通用绘制函数，减少重复代码 |
| 🩹 **空 rows 不画** | 修复：删光股票时空面板时的绘制小 bug |

### v1.4.5

**壁纸自适应**

| 特性 | 说明 |
|---|---|
| 🌓 **浅色壁纸自适应** | 背景透明度 ≤ 120 时自动采样壁纸亮度，浅壁纸切换成深色文字 + 白色描边 |

### v1.3.0

**浅色壁纸自动切深色文字** —— 采样挂件背后壁纸的平均亮度（Rec.601），亮度 > 140 且背景较透明时，全套前景色换成深色版本（近黑文字 / 深红 / 深绿），文字加 1px 白色描边。启动、拖动结束、改透明度时各采一次，之后每 20 秒复查。

### v1.2.3

**收盘彩蛋第三次调优 ——《冰雪奇缘》配色**

| 状态 | 配色来源 | 表现 |
|---|---|---|
| 🔥 **燃烧** | `#FF5500` 橙红（电影点缀色） | 纯橙红底色 + 12 颗亮黄→橙→暗红渐变的火星余烬上升 |
| ❄ **结霜** | `#CCEBE9` / `#75BDB9` / `#cde2ef`（电影主调）+ 爱莎蓝魔法光晕 + `#FCFFFF` 极浅暖白 | 淡青→蓝绿→浅蓝灰渐变 + 中心爱莎蓝魔法光晕缓慢呼吸 + 9 颗雪花 + 3 处六角冰晶 |

### v1.2.0 ~ v1.2.2

v1.2.0 上线了收盘彩蛋。后续三个小版本反复打磨观感：

| 版本 | 火焰 | 冰霜 |
|---|---|---|
| v1.2.0 | 三层火舌 + 火星 + 底色 | 真实冰裂纹（按股票代码生成缓存）+ 霜针 + 冰晶 |
| v1.2.1 | 回退到上一版配色，quadTo 平滑曲线代替 cubicTo 尖锐火舌，12 颗火星 | 同上 |
| v1.2.2 | **取消火焰形状**，只保留红底 + 12 颗火星 | **取消裂纹和锯齿霜针**，颜色更冰蓝更亮 |
| v1.2.3 | #FF5500 橙红点缀色 | 全套《冰雪奇缘》配色 + 爱莎蓝魔法光晕 |
| **v1.2.4** | 同上 | **改为水晶蓝白**（`#E8F2FE` / `#B5D2F0` / `#D6E6FA`），去掉青绿色调 |

### v1.1.0

新增 **多选删除股票**：

- **两种入口**：
  - 在某只股票上**右键** → 直接进入多选模式，并弹出该股的快捷菜单（删除这只 / 编辑持仓 / 全选 / 取消）
  - 在某只股票上**长按 0.5 秒** → 进入多选模式，自动勾上长按的那只
- **多选交互**：每行左侧出现复选框，点击行切换勾选状态；底部出 `取消` + `删除(N)` 两个按钮
- **快捷键**：按 `Esc` 退出多选；多选状态下右键空白也退出
- **主菜单**新增「**多选删除股票…**」入口
- 删除时同时清掉对应股票的持仓成本，免得留下脏数据
- 移动窗口超过 5px 不会触发长按；快速点击不会误触

### v1.0.0

| 特性 | 说明 |
|---|---|
| **真·桌面融入** | 无边框 + 半透明 + 置顶，拖到桌面任意位置；0% 透明时只剩文字浮在壁纸上 |
| **5 只自选股** | 最多 5 只，红涨绿跌（A 股习惯），平盘灰色 |
| **实时刷新** | 1 / 3 / 5 / 10 / 30 秒可选，非交易时段自动降频到 60 秒，省流量省电 |
| **大盘指数** | 顶部常驻上证 / 深证 / 创业板涨跌幅，**不占用** 5 个个股名额 |
| **分时走势图** | 每行右侧迷你分时线，虚线为昨收基准，5 分钟更新一次 |
| **异动提醒** | 涨跌幅达阈值弹系统通知 + 该行呼吸高亮 10 秒，默认 ±3%，同一只 5 分钟内不重复 |
| **持仓盈亏** | 填成本价和股数，直接显示盈亏金额和比例，与当日涨跌分开算 |
| **搜索加自选** | 支持拼音缩写 / 全拼 / 代码 / 中文名，后台线程请求不卡界面 |
| **自定义标题** | 标题可改成任意文字（最多 12 字） |
| **鼠标穿透** | 开启后窗口不再接收鼠标，可压在游戏或全屏窗口上 |
| **开机自启** | 写注册表 `HKCU\...\Run` |
| **系统托盘** | 最小化到托盘，托盘图标实时反映涨跌 |
| **自适应缩放** | 4 档等比缩放，高分屏不糊 |

### 稳定性设计

- 断网自动退避重试，不刷屏报错
- 窗口位置越界自动拉回屏幕内
- 非交易时段自动降频（60 秒一次）
- 全部请求走后台线程，界面永不卡顿

---

## 快速开始

### 方式一：下载即用（推荐）

1. 到 [Releases](../../releases) 页面，下载 **Source code (zip)**
2. 解压到任意目录（建议不要放 C 盘需要权限的位置）
3. 双击 `install.bat`（建 `.venv` 并按 `requirements.txt` 装依赖，**只需一次**）
4. 双击 `start.bat`

> Release 里没有额外的打包产物，只有源码压缩包；依赖装在项目自己的 `.venv` 里，不污染全局 Python，换台机器拷过去也能跑。

### 方式二：从源码运行

```bash
# 1. 建虚拟环境并装依赖（只装一个 PySide6）
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/

# 2. 复制示例配置
cp stocks.example.json stocks.json

# 3. 启动
.venv\Scripts\pythonw.exe widget.py
```

> 也可以直接双击 `install.bat` 单独装依赖。

### 环境要求

- Windows 10 / 11（Windows 11 上效果最佳）
- Python 3.10 – 3.14（只依赖 `PySide6-Essentials==6.11.2`，约 77 MB）
- 装的是 Essentials 而不是完整 `PySide6`：挂件只用 QtCore / QtGui / QtWidgets，完整包会多拖一个 168 MB 的 `PySide6_Addons`（Qt3D / QtCharts / QtWebEngine 等），一个都用不上

---

## 使用说明

### 基本操作

| 操作 | 说明 |
|---|---|
| 左键拖动 | 移动位置（锁定后不可拖） |
| 右键 | 主菜单：编辑股票、刷新间隔、透明度、置顶、穿透、开机自启 |
| 托盘左键 | 显示 / 隐藏窗口 |
| 托盘右键 | 显示隐藏、编辑股票、取消穿透、退出 |

### 改标题

右键 → **修改标题**（最多 12 字），显示在窗口左上角。

### 收盘彩蛋（v1.2.0+）

每个交易日 **14:57** 集合竞价那一刻，程序会给今天的赢家和输家各留 3 分钟仪式感：

| 状态 | 触发 | 表现 |
|---|---|---|
| 🔥 **燃烧** | 涨幅 ≥ 阈值 | `#FF5500` 橙红底色 + 12 颗亮黄→橙→暗红的火星余烬上升 |
| ❄ **结霜** | 跌幅 ≥ 阈值 | 冰雪奇缘配色 + 爱莎蓝魔法光晕 + 9 颗雪花下沉 + 3 处六角冰晶 |

- 持续 **3 分钟**后自动恢复
- 每个交易日**只触发一次**，非交易日不触发
- 触发时托盘会弹一条汇总通知
- 改阈值：右键 →「**收盘彩蛋阈值（14:57）**」→ 关闭 / ±2% / ±3% / ±5%（默认 ±3%）
- **0% 透明模式下彩蛋同样可见**

想提前看效果？跑一下 `python demo.py`，会开一个独立窗口同时展示燃烧和结霜，不碰你的真实配置。

| 三行一一对应 |
|---|
| ![demo](screenshots/preview-demo.png?v=3) |

- 贵州茅台 1500.00 / **+6.21% / 红色** → 🔥 火焰彩蛋
- 平安银行 11.00 / **−5.28% / 绿色** → ❄ 冰霜彩蛋
- 宁德时代 200.00 / +0.55% / 灰色 → 平盘无彩蛋

### 多选删除股票（v1.1.0 新功能）

再也不用手动改代码字符串了：

- **右键某只股票** → 进入多选模式，自动勾上这只，同时弹出该股的快捷菜单（删除这只 / 编辑持仓 / 全选）
- **长按某只股票** 0.5 秒 → 进入多选模式，自动勾上这只
- 进入多选后，每行左侧出现**复选框**，点击行切换勾选
- 底部出两个按钮：**`取消`** / **`删除(N)`**（红色高亮）
- **Esc** 或多选状态下**右键空白**退出多选
- 主菜单也加了「**多选删除股票…**」入口
- 删除时自动清掉对应股票的持仓数据

### 搜索加自选

标题右边的输入框，四种输入都支持：

| 输入 | 示例 | 结果 |
|---|---|---|
| 拼音缩写 | `mt` | 贵州茅台 |
| 全拼 | `pingan` | 中国平安 / 平安银行 |
| 代码 | `600519` | 贵州茅台 |
| 中文名 | `茅台` | 贵州茅台 |

- 输入后自动出候选（防抖 0.3 秒，后台线程请求不卡界面）
- **点击候选或按回车**即可加入自选
- 已经有 5 只时再加，会挤掉最后一只
- `Esc` 清空搜索框并收起候选
- 只返回沪深京 A 股 / 指数 / ETF，港股美股场外基金自动过滤

### 配置备份与恢复（v1.4.9+）

自选股和**持仓成本**都只存在 `stocks.json` 里，删错了很麻烦。现在挂件自己会存档：

- **自动存**：每次改动配置前，先把改动前那份复制进 `backups/`；每次启动也存一份
- **手动存**：右键 →「配置备份」→「立即备份」
- **回滚**：右键 →「配置备份」→ 挑一个时间点，点一下就回到那份配置（覆盖前会先把当前状态也存一份，随时能反悔）
- **看存档**：右键 →「配置备份」→「打开备份目录」

文件名形如 `stocks-20260918-213045.json`。只保留最近 **30** 份，更早的自动删。
只是拖动窗口换个位置不会触发备份（`pos` 不算实质变化）。

> 恢复后建议退出重启挂件，让新配置完全生效。

**万一 `stocks.json` 被改坏了**（手写 JSON 少个括号之类），挂件不会像以前那样静默退回默认配置 —— 它会从 `backups/` 里挑最近一份能解析的自动恢复，并在托盘提示用的是哪份。坏掉的原文件原样留着，不会被覆盖。

### 出问题了怎么办（v1.4.10+）

挂件是 `pythonw` 启动的，**没有控制台窗口**，所以以前一旦出错就是"窗口不见了"，什么痕迹都没有。现在：

1. 出错时会弹一个系统提示框，写明日志路径
2. 日志在 `logs/crash-YYYYmmdd-HHMMSS.log`，里面是完整 traceback，留最近 10 份
3. 也可以主动看：右键 →「配置备份」→「崩溃日志（N）」直接打开日志目录

把那份 `.log` 发出来就能定位问题。

### 菜单项详解

**大盘指数**
窗口顶部一行显示上证 / 深证 / 创业板的涨跌幅，红绿着色，不占用 5 个个股名额。可关。

**异动提醒阈值**
个股涨跌幅绝对值达到阈值时，右下角弹系统通知，同时该行**呼吸高亮 10 秒**。默认 ±3%，可选 ±1 / ±2 / ±3 / ±5% 或关闭。同一只股票 5 分钟内不重复提醒。

**编辑股票代码**
逗号分隔，最多 5 只。三种写法都认：

```
600519          → sh600519
sh600519        → sh600519
600519.SH       → sh600519
```

**编辑持仓成本 / 盈亏**
格式 `代码=成本价[:股数]`，逗号分隔：

```
600519=1250:100    → 茅台成本 1250、持股 100 股，显示盈亏金额 +1066 (+8.5%)
000001=11.5        → 只有成本没股数，显示盈亏比例 -5.76%
```

- 没填成本的股票不显示盈亏
- 盈亏按 A 股习惯着色：**盈利红、亏损绿**
- **与当日涨跌分开算**——当天跌但持仓仍盈利，就是绿的涨跌幅配红的盈亏

**刷新间隔**
1 / 3 / 5 / 10 / 30 秒。非交易时段自动降为 60 秒一次。

**背景透明度**
0% ~ 100%。详见上方"不同透明度对比"。

**界面大小**
85% / 标准 / 115% / 130%。等比缩放。

**鼠标穿透**
窗口不再接收鼠标，适合压在游戏 / 全屏窗口上。开启后**必须靠托盘菜单关闭**（否则就点不到了）。

**开机自启**
写入注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run`。

---

## 配置

同目录 `stocks.json`，可直接手改后重启。首次使用请从 `stocks.example.json` 复制一份。

```json
{
  "title": "A股盯盘",
  "show_index": true,
  "alert_pct": 3.0,
  "effect_pct": 3.0,
  "codes": ["sh600519", "sz000001", "sz300750"],
  "positions": { "sh600519": { "cost": 1250.0, "shares": 100 } },
  "interval": 3,
  "bg_alpha": 190,
  "ui_scale": 1.0,
  "locked": false,
  "spark": true,
  "click_through": false,
  "always_on_top": true,
  "autostart": false,
  "pos": null
}
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `title` | string | 窗口左上角标题，最多 12 字 |
| `show_index` | bool | 是否显示大盘指数 |
| `alert_pct` | float | 异动提醒阈值（百分点），`0` 关闭 |
| `effect_pct` | float | 收盘彩蛋阈值（百分点）：14:57 时涨幅超 → 燃烧，跌幅超 → 结霜，持续 3 分钟；`0` 关闭 |
| `codes` | array | 自选股代码，最多 5 个 |
| `positions` | object | 持仓，`{"代码": {"cost": 成本, "shares": 股数}}`，`shares` 可省略 |
| `interval` | int | 刷新间隔（秒） |
| `bg_alpha` | int | 背景不透明度 0-255，`0` 为完全透明 |
| `ui_scale` | float | 界面缩放，0.85 / 1.0 / 1.15 / 1.30 |
| `locked` | bool | 锁定位置，不可拖动 |
| `snap` | bool | 拖动时吸附到屏幕边缘（16px 内），默认 `true` |
| `spark` | bool | 是否显示分时走势图 |
| `click_through` | bool | 鼠标穿透 |
| `always_on_top` | bool | 窗口置顶 |
| `autostart` | bool | 开机自启 |
| `pos` | array | 窗口位置 `[x, y]`，`null` 为自动贴右上角 |

> ⚠️ `stocks.json` 含个人自选股和持仓成本，已在 `.gitignore` 中排除，**不会被提交到仓库**。
> 同目录的 `backups/` 是自动存档目录，同样不入库。

---

## 开发 / 测试

```bash
python run_tests.py              # 跑全部
python tests/test_clock.py       # 也可以只跑一个文件
```

| 文件 | 覆盖 |
|---|---|
| `test_regress.py` | 综合回归：渲染 / 菜单 / 找窗口 / 单实例 / 运行时开关状态 |
| `test_market_core.py` | 行情核心：代码归一化 / 完整代码身份 / 漏返回与乱序 / 脏数据 |
| `test_provider_fixtures.py` | 用真实接口返回跑解析：字段位置漂移 / 品种覆盖 / 搜索过滤 |
| `test_async_state.py` | 异步状态：generation 丢弃旧行情 / 搜索 seq / 空自选 / 提醒去重 |
| `test_clock.py` | 交易时段：7 个阶段 / 北京时间 / 2026 官方休市日 / 截至日期 |
| `test_source.py` | 数据源：涨跌停推算 / 停牌判定 / 代码分类 / 降级调度 / 老板键 |
| `test_release_hygiene.py` | 发布卫生：私有路径 / token / 版本一致性 / 同类同名方法 |
| `test_config_schema.py` | 配置校验：脏类型 / 越界 / 可选集合 / 原子写入 / worker 停机 |
| `test_calendar.py` | 日期触发规则：触发日算法 / 单激活 / 表完整性 |
| `test_backup.py` | 配置备份：生成 / 去重 / 轮转 / 恢复 / 坏文件 |
| `test_crash.py` | 崩溃兜底：日志落盘 / 轮转 / 三层 hook / main 兜底 |
| `test_snap.py` | 边缘吸附：四边 / 阈值边界 / 四角 / 开关 / 多显示器 |
| `test_recover.py` | 配置损坏自动回滚：损坏恢复 / 备份全坏 / 全新安装 |

几个约定：

- **全部在临时目录里跑**，不碰真实的 `stocks.json` / `backups/` / `logs/`，跑完还会反过来断言"真实配置没变"
- `offscreen` 平台，不需要显示器
- 用例全过但**进程退出码非 0 也算失败** —— 专门用来抓"QThread 还在跑就被销毁，Qt 直接 abort（0xC0000409）"这类退出时的问题
- 建 `Ticker` 之前要先屏蔽 `Fetcher.start` / `Searcher.start`，否则线程会在退出时把进程带崩
- `tests/fixtures/` 存的是**真实抓回来的接口响应**（`test_provider_fixtures.py` 用它跑解析）。接口改字段顺序会立刻红 —— 手写样例做不到这点，因为接口改了它也照样通过

### tools/ 小工具

```bash
python tools/selfcheck.py        # 静态自检：未使用 import / 死代码 / 编译 / 超长函数
python tools/release_check.py    # 发版前检查：测试 / 自检 / 编译 / 版本一致 / 无私有路径
python tools/capture_fixtures.py # 重抓一份真实接口响应，存进 tests/fixtures/
python tools/measure_size.py     # 体积报告（行数 / 字节数 / 运行时内存快照）
python tools/usage_sampler.py --pid <PID> --duration 1800 --interval 30
                                 # 30 分钟占用采样 → usage_report.csv / .json
python tools/thread_probe.py --pid <PID> --duration 120
                                 # 线程级 CPU：到底是哪几个线程在烧 CPU
python tools/detect_garbled.py screenshots/   # 扫一遍截图，看有没有「字体缺字方块」
python tools/push_github.py      # 走 GitHub Git Data API 推送（绕开代理对 github.com:443 的封锁）
```

## 数据源

公开行情接口，无需 Key。默认**自动**，主源挂了自动降级到备源：

| 源 | 实时行情 | 分时数据 | 股票搜索 |
|---|---|---|---|
| 腾讯财经（主） | `qt.gtimg.cn` | `web.ifzq.gtimg.cn` | `smartbox.gtimg.cn` |
| 新浪财经（备） | `hq.sinajs.cn` | `quotes.sina.cn` | `suggest3.sinajs.cn` |

- 右键 →「数据源」可手动指定：自动 / 腾讯 / 新浪
- 自动模式会记住上次成功的源，下次优先用它（少一次失败往返）
- 两个源字段不一样（新浪没有涨跌停价，按板块规则推算），已在 `providers.py` 里归一
- 行情有约 0~3 秒延迟，仅供参考，不构成投资建议

---

## 隐私

- **不采集、不上传任何数据。** 没有埋点、没有使用统计、没有崩溃自动上报。
- 唯一的对外网络请求是**拉取行情**：只 GET 上面那几个公开接口，请求里不带任何本机信息。
- 自选股和持仓成本只存在本机的 `stocks.json`，**默认不入库**（`.gitignore` 已排除）。
- 崩溃日志只写本机 `logs/`，不会自动发送 —— 要反馈得你自己拿出来。
- 开机自启只写当前用户的注册表 Run 项，不碰系统目录，不需要管理员权限。
- 源码完全公开，可以自己核对以上每一条。

---

## 体积 / 占用

> 数据由 `python tools/measure_size.py` 现测，不是估的。

### 主程序

| 项目 | 数值 |
|---|---|
| `widget.py` 源文件 | **113.1 KB**（2883 行） |
| ├ 有效代码行 | 2430 行 |
| ├ 注释行 | 110 行（4%） |
| └ 空行 | 343 行 |
| 编译字节码 `.pyc` | 170.9 KB（运行缓存，删了会自动重建） |

### 目录

| 目录 | 大小 | 文件数 | 运行要不要 |
|---|---|---|---|
| `tests/` | 98 KB | 22 | ❌ 开发用 |
| `tools/` | 71 KB | 16 | ❌ 开发用 |
| `screenshots/` | 749 KB | 17 | ❌ 只给 README 配图 |

**真正跑起来只需要 `widget.py` 135 KB + `stocks.json`（首次运行自动生成）。**

第三方依赖只有 **PySide6** 一个（那是环境依赖，不算本项目体积）。

### 运行时内存

30 分钟实测见文末《30 分钟占用报告》。快照：RSS 约 108 MB，峰值 159 MB，提交内存 71.6 MB。
> 108 MB 听着不小，但其中绝大部分是 PySide6 / Qt 运行时本身（导入 `PySide6.QtWidgets` 后空进程就有 ~90 MB）。本项目自己的数据结构只有几十 KB 量级。

### CPU 是单线程还是多核？

**是多线程进程，但不需要多核。** 实测（120 秒线程级采样，20 核机器）：

| 指标 | 数值 |
|---|---|
| 进程 CPU（单核 = 100% 基准）均值 | **0.73%** |
| 进程 CPU（20 核均摊）均值 | 0.036% |
| 最忙线程（主线程，负责 UI/重绘）占总 CPU | 73.5% |
| 是否有线程吃满一个核 | ❌ 没有，峰值也只有 1.04% |

进程里有 6~10 个线程（主线程 + 行情拉取 + 搜索 + Qt 内部线程），但工作线程几乎全程在等网络或定时器，CPU 消耗集中在主线程的重绘上。
**负载只有单核的 0.73%，这时候去"全员调动 20 个核"纯属浪费电** —— 真要让所有核一起转，反而会因为核间同步和唤醒开销把这点活干得更慢更费。
真出现 CPU 瓶颈时（比如某天要画几千个粒子），正确的做法是拆成多进程或用 Qt 的线程池，而不是让 20 个核空转。

v2.0.0 为此做的降频：平时心跳 500 ms（2 fps）、有动画才 50 ms（20 fps）、窗口不可见完全不画、`QColor` 带缓存复用。

---

## 30 分钟占用报告

> 实测，不是估的。挂件开着跑了 30 分钟，每 30 秒采一个点，共 60 个点。
> 采样脚本 `tools/usage_sampler.py`，出报告 `tools/report_usage.py`，原始数据留在 `tools/usage_report.csv` / `.json`。
> 机器：20 核（可调度 20 核），Windows。

### CPU

| 指标 | 均值 | 峰值 | 最低 |
|---|---|---|---|
| 进程 CPU（单核 = 100% 基准） | **0.164%** | 4.270% | 0.010% |
| 进程 CPU（20 核均摊） | 0.007% | 0.210% | 0.000% |
| 整机系统 CPU（所有核合计，含别的程序） | 14.795% | 197.28% | 1.59% |

30 分钟里，挂件平均只用了**单核的 0.164%**。峰值 4.27% 出现在刚启动的头 30 秒（加载 + 首屏绘制），之后一路贴着 0。
作为对比，同一时间整机的系统 CPU 均值是 14.8% —— **挂件的占用是整机的 1/90**。

### 内存

| 指标 | 起始 | 结束 | 均值 | 峰值 | 变化 |
|---|---|---|---|---|---|
| 工作集 RSS | 158.2 MB | **108.3 MB** | 118.6 MB | 158.3 MB | **−49.9 MB** |
| 提交内存 Pagefile | 70.8 MB | 71.6 MB | 71.5 MB | 71.7 MB | +0.8 MB |

- RSS 不但没涨，**还自己降了 50 MB**：启动瞬间要加载 Qt + 建窗口，之后 Python GC 把一次性对象回收掉，稳定在 108 MB 就不再动了
- 提交内存（真正占的虚拟内存）全程 71 MB 上下，纹丝不动
- **没有内存泄漏**。30 分钟都在 107.6 ~ 108.3 MB 这条线上平着走，连 1 MB 波动都没有

> 108 MB 里绝大部分是 PySide6 / Qt 运行时本身 —— 一个空的 `import PySide6.QtWidgets` 进程就是 90 MB 上下。挂件自己的数据结构只有几十 KB。

### 内核对象 / 线程（泄漏检测）

| 指标 | 起始 | 结束 | 均值 | 峰值 | 变化 | 判定 |
|---|---|---|---|---|---|---|
| 句柄数 | 1007 | 1008 | 1002.9 | 1013 | +1 | ✅ 稳定 |
| GDI 对象 | 45 | 47 | 46.9 | 47 | +2 | ✅ 稳定 |
| USER 对象 | 48 | 50 | 47.4 | 52 | +2 | ✅ 稳定 |
| 线程数 | 12 | 10 | 8.1 | 12 | −2 | ✅ 稳定 |

**GDI 对象是 Qt 桌面程序最典型的泄漏点**（画刷、画笔、字体没释放），30 分钟只 +2 且早就到顶不再涨，说明绘制路径是干净的。句柄 +1 同理。
线程数在 6~12 之间来回（搜索线程是临时开的，用完就退），均值 8.1 —— 没有线程泄漏。

### 一句话

挂件挂着跑 30 分钟：**CPU 占单核 0.16%，内存稳定 108 MB 不涨，GDI / 句柄零泄漏。** 开着睡觉完全没问题。

---

## 致谢 / Credits

这个小挂件能跑起来，靠的是下面这些项目和作者，一并谢过：

- **[Qt for Python (PySide6)](https://doc.qt.io/qtforpython/)** —— The Qt Company 出品的 Python 绑定，LGPLv3 授权。整个界面都是用它的 `QPainter` 手绘的（无边框、半透明、逐帧动画全靠它）
- **[Qt](https://www.qt.io/)** —— 底层的 C++ 框架。感谢 Qt Company 与 KDE/Qt 社区多年维护
- **[腾讯财经](https://gu.qq.com/)** —— 免费公开的行情接口（`qt.gtimg.cn` / `web.ifzq.gtimg.cn` / `smartbox.gtimg.cn`），无需注册、无需 Key，个人看盘够用
- **[Python](https://www.python.org/)** 与 **[PSF](https://www.python.org/psf/)** —— 标准库里的 `ctypes` / `json` / `urllib` / `threading` / `traceback` 让这个项目不需要任何额外的第三方依赖
- **[Microsoft YaHei](https://learn.microsoft.com/typography/)** —— 界面字体

> 免责声明：本项目只是个人看盘小工具，**不提供任何投资建议**。行情数据来自第三方公开接口，可能有延迟或错误，请以券商/交易所数据为准。使用者自负盈亏。

---

## 常见问题

**Q：双击 `start.bat` 没反应？**
先双击 `install.bat` 单独装依赖，看有没有报错。或者手动 `pip install PySide6`。

**Q：窗口不见了？**
看系统托盘（右下角箭头里），左键点图标显示 / 隐藏。

**Q：开启鼠标穿透后点不到了？**
右键托盘图标 → 取消穿透。

**Q：0% 透明下字看不清？**
程序已自动加描边阴影。如果壁纸特别花，建议调到 25% 或更高。

**Q：能盯港股美股吗？**
不能，只做沪深京 A 股 / 指数 / ETF。搜索时港股美股场外基金已自动过滤。

**Q：数据准吗？**
接口有 0~3 秒延迟，仅供看盘参考，不构成投资建议。

---

## 更新日志

### v2.0.1

安全收紧 + 文档图修复。

- **退出时清空手动开关**：退出即清，下次启动严格按日期触发
- **修复 `_singleton_check()` 真 bug**：检测到已存在时漏 CloseHandle，导致别人退出后 mutex 引用计数不归零，**永远误报"已在运行"**
- **修复文档图乱码**：offscreen 渲染时中文 + emoji 全部方块（之前用真实 GUI 截的图没事）；文档工具注入系统字体 + Segoe UI Emoji 后重生成
- **文档脱敏**：内部细节从公开文档移除
- 新增 `tools/push_github.py`：走 GitHub Git Data API 推送（绕开代理对 `github.com:443` 的封锁）
- 新增 `tools/detect_garbled.py`：程序化检测截图里的「字体缺字方块」，避免文档图出现乱码
- 推 GitHub 前**全项目脱敏检查**：自选股代码 / 用户名 / API key 全部 0 处
- 测试 152 → 161 项

### v2.0.0

内部重构 + 瘦身。

**1. 内部重构**

完成多项内部结构整理。涉及内部规则的部分不写进公开文档。

**2. 去屎山 / 降 CPU / 省内存**

- 删掉 3 个重复判断方法 + 123 行旧画法代码，画法函数从「Ticker 的方法」改成**模块级函数**（不碰 self，少一层开销）
- 删掉没人用的 `fmt_time()` 和没用到的 `QSize` import；某个不再需要的常量也一并清理
- 空闲心跳 200ms → **500ms**，动画 25fps → **20fps**（唤醒次数少 20%，肉眼几乎无差）
- 隐藏到托盘 / 窗口不可见时 `_need_anim()` 直接返回 False —— 看不见就不画
- `QColor` 全部走 `_col()` 缓存，一帧省下几十上百个对象分配
- `run_tests.py` 把"用例全过但退出码非 0"也判失败，专门抓退出时才炸的问题

**3. 测试**

149 项，全部通过。

**4. 自检工具链**

| 脚本 | 干什么 |
|---|---|
| `tools/selfcheck.py` | 静态自检：未使用的 import / 没人引用的顶层名字 / 全量编译 / 超长函数 |
| `tools/measure_size.py` | 体积报告：行数 / 字节数 / 目录占用 / 运行时内存快照 |
| `tools/usage_sampler.py` | 长时间占用采样（CPU / RSS / 句柄 / GDI / USER / 线程 / 系统 CPU）→ csv + json |
| `tools/report_usage.py` | 把采样 csv 整理成人能读的 markdown 报告 |
| `tools/thread_probe.py` | 线程级 CPU：到底是哪几个线程在烧 CPU，有没有线程吃满一核 |

`selfcheck.py` 跑出来：未使用 import 0 个、没人引用的顶层名字 0 个、全部文件编译通过。
（历史债记一笔：`Ticker` 类 1565 行偏大，暂时没拆 —— 拆它风险大于收益，测试覆盖到就行。）

**8. 其他**

- README 补《体积 / 占用》《30 分钟占用报告》《致谢 / Credits》三节
- `APP_VERSION` → `v2.0.0`

### v1.4.13

配置损坏自动回滚：

- **起因**：`load_config()` 原来 `except: pass` —— 文件读坏了就静默退回 `DEFAULT_CONFIG`，
  自选股和持仓**凭空消失**，连个提示都没有。v1.4.9 有了备份，就该让它自己捞回来
- **`recover_from_snapshot()`**：按时间倒序遍历快照，逐个 `json.loads` 验，挑**最近一份能用的**；
  来源记进 `_recovered_from`，`Ticker` 启动 1.5s 后弹托盘提示（说清楚用的是哪份）
- **坏文件原样保留**：不覆盖、不删除、不改名（留给用户自己查）
- **`snapshot_config()` 拒收坏 JSON**：`json.loads` 不过就不存档 —— 否则垃圾会占掉备份位，
  而且下次恢复遍历时还得跳过
- **文件不存在 ≠ 文件损坏**：全新安装走默认配置，不会把用户删掉的旧配置复活
- 验证脚本 `_recover_check.py` 15/15（正常读取 / 损坏恢复 / 备份也全坏 / 垃圾不进备份 /
  挑最近可用的 / 全新安装不翻旧账）；五套共 107 项
- `APP_VERSION` → `v1.4.13`

### v1.4.12

单实例唤醒认错窗口（真 bug）：

- 原来 `_bring_existing_to_front()` 用 `FindWindowW(None, 标题)` 找旧窗口。**标题是用户自己设的**，实测撞上了**微信会话窗口**（`Weixin.exe`，如果挂件的标题被设成跟某条微信会话同名，微信也是 Qt 写的，连窗口类名 `Qt5xxxxQWindowIcon` 都一样，按类名也筛不掉）
- 后果：重复启动挂件时，被弹到前台的可能是**微信**而不是挂件
- 改法：`find_widget_window()` 枚举所有同名窗口 → 取 PID → `QueryFullProcessImageNameW` 查进程名 → 只认 `python(w).exe`
- 验证：`_regress.py` 32/32（新增"命中的是 python 进程，不是微信"）
- `APP_VERSION` → `v1.4.12`

### v1.4.11

拖动吸附屏幕边缘：

- **`snap_pos(x, y, cursor)`**：距边缘 `SNAP_MARGIN=16` 以内就贴边；左右上下独立判断，所以四角会同时吸
- **多显示器**：用 `QApplication.screenAt(cursor)` 取光标所在屏，退回 `self.screen()` → 主屏
- **拖动中实时吸 + 松手再吸一次**：`mouseMoveEvent` 里吸（有磁吸手感），`mouseReleaseEvent` 里再吸一次（避免最后一段位移把窗口带偏）。`drag_pos` 是按下时的固定偏移，所以吸附不会造成累积漂移
- 配置 `snap`（默认开）+ 菜单「拖动吸附屏幕边缘」；开启时立刻吸一次当前位置
- 位置越界保护是**之前就有的**（启动时校验 `pos` 是否还在某块屏幕内，不在就贴回右上角），这次没动
- 验证脚本 `_snap_check.py` 18/18（四边/阈值边界/四角/开关/多显示器入参/不污染真实配置）
- `APP_VERSION` → `v1.4.11`

### v1.4.10

崩溃兜底。起因：`pythonw` 没控制台，异常 = 静默消失，无从查起。

- **`logs/crash-*.log`**：`write_crash_log()` 落盘，含 `APP_VERSION` / Python 版本 / 时间 / 完整 traceback，留最近 10 份；同一秒崩多次用 `-2/-3` 后缀
- **三层兜底**：`sys.excepthook`（主线程 + Qt 槽）→ `threading.excepthook`（子线程）→ `Fetcher.run` / `Searcher.run` 内部各包一层（`_loop()`）。QThread 里冒出去的异常**不一定**走 `sys.excepthook`，所以线程自己必须兜
- **弹窗用 Win32 `MessageBoxW`**：Qt 自己崩的时候 QMessageBox 弹不出来
- **`main()` 包 try/except**：启动阶段炸了也有日志，退出码 1；`SystemExit` 原样放行不吞
- 菜单「配置备份」→「崩溃日志（N）」打开日志目录
- 顺手删掉 `_singleton_check()` 里一段 `return False` 之后的**死代码**（上次编辑残留的重复 mutex 块）
- 验证脚本 `_crash_check.py` 19/19；三套合计 69 项全过
- `APP_VERSION` → `v1.4.10`

### v1.4.9

配置自动备份（起因：持仓成本被临时脚本洗掉过 3 次，没存档找不回来）：

- **自动快照**：`save_config()` 写之前先调 `snapshot_config()`，把「改动前」那份存进 `backups/`；`main()` 启动时也存一份
- **去重**：与最近一份实质相同就不存（`_essence()` 比的是去掉 `pos` 的 JSON），所以拖窗口不会刷屏
- **轮转**：最多留 30 份，超出的删最旧。同一秒内的多份用 `-2 / -3` 后缀，排序按 `(日期, 时间, 序号)`，序号取已有最大值 +1 —— 轮转删掉的号不复用，否则新快照会反复覆盖旧号，越存越旧
- **回滚**：右键 →「配置备份」列出最近 8 份，恢复前会先 `json.loads` 验一遍（坏文件写不进去），并自动存一份当前状态
- **单实例保护**：`CreateMutexW("Local\\AStockWidget-Singleton-v1")`，已在运行则把旧窗口 `ShowWindow(SW_RESTORE)` + `SetForegroundWindow` 唤回来。模块级 `_singleton_locked` 防止进程内重复调用误判自己
- 验证脚本 `_backup_check.py` 23 项全通过（含"真实 backups/ 未被测试污染"）
- `APP_VERSION` → `v1.4.9`

### v1.4.8

绘制与日期计算的内部改进。

- `APP_VERSION` → `v1.4.8`

### v1.4.6

绘制逻辑整理：

- 抽出通用绘制路径，减少重复代码
- **修复**：删光股票时空面板时的绘制小 bug
- `APP_VERSION` → `v1.4.6`

### v1.4.5

细节打磨 + 绘制开关收敛。

- `APP_VERSION` → `v1.4.5`

### v1.4.2

绘制连续性优化：

- 粒子数与 alpha 包络调整，过渡更平滑
- 去掉左上角的手绘元素，恢复成状态点 + 标题

### v1.4.0

绘制框架落地。

### v1.3.0

- **浅色壁纸自适应**：`sample_bg()` 用 `QScreen.grabWindow()` 抓挂件区域（分层窗口抓不到，正好拿到的就是壁纸），算平均亮度；亮度 > 140 且 `bg_alpha ≤ 120` 时切换全套深色前景 + 1px 白色描边
- 采样时机：启动 600ms、拖动结束 250ms、改透明度 150ms、之后每 20 秒
- `APP_VERSION` → `v1.4.5`

### v1.2.4

- 冰霜改为**水晶蓝白**：`#E8F2FE` → `#B5D2F0` → `#D6E6FA`，中心白色高光淡出到宝蓝 `#5B95E8`，描边 `#5B95E8`。去掉 v1.2.3 的青绿色调

### v1.2.3

收盘彩蛋第三次打磨 —— 套用迪士尼《冰雪奇缘》配色：

- **冰霜**：渐变改为 `#CCEBE9` 淡青 → `#75BDB9` 蓝绿 → `#cde2ef` 浅蓝灰；新增**爱莎蓝魔法光晕**（`QRadialGradient(160,173,215)`）缓慢呼吸；反光、雪花、冰晶统一用 `#FCFFFF` 极浅暖白；描边色 `#8cbde0` 淡蓝
- **火焰**：辉光 / 暖光 / 描边统一 `#FF5500` 橙红点缀色，与冰霜同源配色
- 文字描边：frost 行用 `#133577` 深蓝（电影海报色），fire 行保持深红
- `APP_VERSION` → `v1.2.3`

### v1.2.2

收盘彩蛋观感大幅简化：

- **火焰**：删除火苗 / 火舌 / crest 高光路径，只保留红底 + 12 颗上升火星（亮黄→橙→暗红渐变）
- **冰霜**：删除裂纹动画（`_crack_paths` / `_draw_crack` / `_crack_cache` 一起清理）和上下 / 左右锯齿霜针；颜色更冰蓝（降绿升蓝）、整体亮度上调；雪花 6→9 颗

### v1.2.1

- 火焰回退配色到 `#ff2600 → #ff8a10 → #ffea84`，用 quadTo 平滑曲线替代 cubicTo 尖锐火舌
- 火星粒子 8→12 颗、size 1.9→2.3，沿整行高度上升
- `_text()` 加 `outline` 参数，火焰 / 冰霜行的 5 处文字加深色描边保证可读

### v1.2.0

- 新增**收盘彩蛋**
  - 14:57 集合竞价定格，涨幅超阈值 → 格子燃烧 3 分钟；跌幅超阈值 → 结霜 3 分钟
  - 火焰：三层火舌 + 底部热辐射 + 上升火星余烬，伪噪声驱动
  - 冰霜：按代码生成并缓存的真实冰裂纹（主纹 + 树枝状分支），1.5 秒生长冻开 + 下沉霜粒 + 冰面反光
  - 每个交易日只触发一次，非交易日不触发，触发时托盘汇总通知
  - 阈值可调（关闭 / ±2% / ±3% / ±5%），默认 ±3%
  - 0% 透明模式下彩蛋依然可见
- 新增 `effect_pct` 配置项

### v1.1.0

- 新增多选删除股票
  - 右键股票进入多选模式，同时弹出该股快捷菜单
  - 长按 0.5 秒进入多选，自动勾上长按的那只
  - 每行左侧复选框，点击切换勾选
  - 底部 `取消` / `删除(N)` 按钮
  - Esc 退出，主菜单新增入口
- 删除股票时自动清掉对应持仓数据
- 移动窗口超过 5px 不会误触长按

### v1.0.0

- 首个正式版本
- 半透明桌面挂件，最多 5 只自选股
- 大盘指数常驻、分时走势图、异动提醒
- 持仓成本与盈亏计算
- 搜索加自选（拼音 / 代码 / 中文名）
- 自定义标题、4 档界面缩放
- 0% 完全透明模式，文字自动描边
- 鼠标穿透、开机自启、系统托盘

---

## 免责声明

本项目仅供学习和个人看盘使用，数据来源为第三方公开接口，不保证实时性和准确性。**不构成任何投资建议，据此操作风险自负。**

## 许可

MIT License
